/* src/components/ui/Icon/constants.js */

/**
 * Predefined size mappings for standard icons.
 * @type {Record<string, number>}
 */
export const ICON_SIZE_MAPS = {
  sm: 16,
  md: 24,
  lg: 32,
  xl: 48,
};

/**
 * Predefined size keys.
 * @type {Record<string, string>}
 */
export const ICON_SIZES = {
  SM: 'sm',
  MD: 'md',
  LG: 'lg',
  XL: 'xl',
};

/**
 * Type-safe configuration object for custom brand icon names.
 * These correspond to local custom SVG assets in src/assets/icons/custom/.
 * @type {Record<string, string>}
 */
export const CUSTOM_ICON_NAMES = {
  SHIELD_CHECK: 'shield-check',
  AWARD_MEDAL: 'award-medal',
  USER_SUPPORT: 'user-support',
  OFFICE_BUILDING: 'office-building',
  HOSPITAL: 'hospital',
  SHIELD_SEARCH: 'shield-search',
  TARGET_PIN: 'target-pin',
  DOCUMENT_REPORT: 'document-report',
  ADD_TEAM: 'add-team',
  STOREFRONT: 'storefront',
  TECHNICIAN: 'technician',
  USER_STAR: 'user-star',
  TOOLBOX: 'toolbox',
  WHATSAPP: 'whatsapp',
};
