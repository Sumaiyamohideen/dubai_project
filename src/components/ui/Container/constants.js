/* src/components/ui/Container/constants.js */

/**
 * Type-safe configuration object for container max-widths.
 * Prevents string typos during component usage.
 * @type {Record<string, string>}
 */
export const CONTAINER_WIDTHS = {
  STANDARD: 'standard',
  NARROW: 'narrow',
  WIDE: 'wide',
};
